# Default configuration for local development and this coding challenge.
# NOTE: This file is committed so that `docker compose up` works on a fresh machine.

# App
PORT=5000
LOG_LEVEL=info

# Database (for running the app without Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tibber
DB_USER=postgres
DB_PASSWORD=postgres
